# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/

# 중간 파싱 결과 (chunks에서 재생성 가능, 불필요하게 큼)
dataset/parsed_text/

# STM32CubeMX MCU DB raw (~600MB, ST 라이선스 자산). 커밋 금지 —
# scripts/parse_cubemx_db.py 로 agent/pin_function_options.json만 파생·커밋한다.
dataset/STM32CubeMX/

# Qdrant 벡터 DB (Docker volume — git으로 관리 안 함, Spark에서 embed_and_index.py 실행)
qdrant_storage/
qdrant_snapshots/

# IDE / OS
.idea/
.vscode/
*.DS_Store
*.swp
~$*
.~lock.*

# 임시 파일
files.zip
*.tmp
*.log

# 빌드
dist/
build/

# Step3 코드 RAG 청크 — opensource 알고리즘 코드 발췌(특히 GPL) 포함.
# 라이선스상 리포에 커밋 금지. parse_opensource_algorithms.py로 로컬 재생성.
dataset/chunks_code/
